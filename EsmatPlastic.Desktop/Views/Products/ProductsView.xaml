<UserControl x:Class="EsmatPlastic.Desktop.Views.Products.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="المنتجات"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock Text="إدارة المنتجات والأصناف"
                           FontSize="14"
                           Foreground="#666666"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    x:Name="AddButton"
                    Content="+  إضافة منتج"
                    Width="145"
                    Height="42"
                    FontSize="14"
                    Click="AddButton_Click"/>
        </Grid>

        <!-- Products List -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Margin="5,0,5,10">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="130"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="رقم المنتج"
                               FontWeight="Bold"/>

                    <TextBlock Grid.Column="1"
                               Text="اسم المنتج"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="2"
                               Text="الوصف"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="3"
                               Text="الحالة"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Grid>

                <ListView Grid.Row="1"
                          ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                          BorderThickness="0">

                    <ListView.ItemTemplate>
                        <DataTemplate>

                            <Border Padding="8"
                                    Margin="0,3"
                                    BorderBrush="#E5E7EB"
                                    BorderThickness="0,0,0,1">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="130"/>
                                        <ColumnDefinition Width="130"/>
                                        <ColumnDefinition Width="130"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"/>

                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="12"
                                                   Foreground="#777777"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding VariantCount}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center">

                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text"
                                                        Value="غير نشط"/>
                                                <Setter Property="Foreground"
                                                        Value="#999999"/>

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}"
                                                                 Value="True">
                                                        <Setter Property="Text"
                                                                Value="نشط"/>
                                                        <Setter Property="Foreground"
                                                                Value="Green"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>

                                    </TextBlock>

                                    <Button Grid.Column="3"
                                            Content="تعديل"
                                            Width="90"
                                            Height="32"
                                            HorizontalAlignment="Center"
                                            Click="VariantsButton_Click"
                                            Tag="{Binding}"/>

                                </Grid>
                            </Border>

                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="3"
              Margin="5,12,5,0">

            <TextBlock x:Name="StatusText"
                       Text="{Binding StatusMessage}"
                       Foreground="#666666"
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button x:Name="EditButton"
                        Content="تعديل"
                        Width="100"
                        Height="38"
                        Margin="0,0,8,0"
                        Click="EditButton_Click"/>

                <Button x:Name="DeleteButton"
                        Content="حذف"
                        Width="100"
                        Height="38"
                        Click="DeleteButton_Click"/>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>
