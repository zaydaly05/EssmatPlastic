<UserControl x:Class="EsmatPlastic.Desktop.Views.OrderRequests.OrderRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Request Form -->
        <Border Grid.Column="0" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="طلب حجز منتجات" Style="{StaticResource PageTitle}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="اسم العميل" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="CustomerNameInput" Margin="0,0,0,15"/>

                <TextBlock Text="رقم الهاتف" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="CustomerPhoneInput" Margin="0,0,0,15"/>

                <Separator Margin="0,10" Background="{DynamicResource LineBrush}"/>
                <TextBlock Text="المنتجات المطلوبة" Style="{StaticResource FieldLabel}" Margin="0,10,0,10"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="ProductVariantCombo" Grid.Column="0" Margin="0,0,5,0"/>
                    <TextBox x:Name="QuantityInput" Grid.Column="1" Margin="0,0,5,0" Text="1"/>
                    <Button Content="+" Style="{StaticResource PrimaryButton}" Grid.Column="2" Width="40" Click="AddItem_Click"/>
                </Grid>

                <ListView x:Name="RequestedItemsList" Height="200" Margin="0,10">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="المنتج" DisplayMemberBinding="{Binding VariantName}" Width="150"/>
                            <GridViewColumn Header="الكمية" DisplayMemberBinding="{Binding Quantity}" Width="80"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <Button Content="إرسال طلب الحجز" Style="{StaticResource PrimaryButton}"
                        Margin="0,20,0,0" Height="45" Click="SubmitRequest_Click"/>
            </StackPanel>
        </Border>

        <!-- Requests List -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="طلبات الحجز الحالية" Style="{StaticResource PageTitle}" Margin="0,0,0,20"/>

            <DataGrid x:Name="OrdersDataGrid" Grid.Row="1"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         Background="Transparent"
                         BorderThickness="0"
                         RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="150"/>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding RequestedAt}" Width="120"/>
                    <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100"/>
                    <DataGridTemplateColumn Header="إجراءات" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="إلغاء" Style="{StaticResource DangerButton}"
                                        Click="CancelRequest_Click" Tag="{Binding Id}"
                                        Padding="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
