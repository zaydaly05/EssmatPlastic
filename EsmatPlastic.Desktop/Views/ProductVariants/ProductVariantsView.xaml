<UserControl x:Class="EsmatPlastic.Desktop.Views.ProductVariants.ProductVariantsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>

                <TextBlock Text="أصناف المنتج"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock x:Name="ProductNameText"
                           FontSize="15"
                           Foreground="#666666"
                           Margin="0,6,0,0"/>

            </StackPanel>

            <Button x:Name="AddButton"
                    Grid.Column="1"
                    Content="+  إضافة صنف"
                    Width="140"
                    Height="42"
                    FontSize="14"
                    Click="AddButton_Click"/>

        </Grid>


        <!-- Variants -->

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="15">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <!-- Header -->

                <Grid Grid.Row="0"
                      Margin="5,0,5,10">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="رقم الصنف"
                               FontWeight="Bold"/>

                    <TextBlock Grid.Column="1"
                               Text="اسم الصنف"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="2"
                               Text="المقاس"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="3"
                               Text="اللون"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="4"
                               Text="الغطاء"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="5"
                               Text="المادة"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                </Grid>


                <!-- List -->

                <ListView Grid.Row="1"
                          ItemsSource="{Binding Variants}"
                          SelectedItem="{Binding SelectedVariant, Mode=TwoWay}"
                          BorderThickness="0">

                    <ListView.ItemTemplate>

                        <DataTemplate>

                            <Border Padding="8"
                                    Margin="0,3"
                                    BorderBrush="#E5E7EB"
                                    BorderThickness="0,0,0,1">

                                <Grid>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>


                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Name}"
                                               FontSize="15"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Size}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Color}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="3"
                                               Text="{Binding CapType}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="4"
                                               Text="{Binding Material}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="5"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center">

                                        <TextBlock.Style>

                                            <Style TargetType="TextBlock">

                                                <Setter Property="Text"
                                                        Value="غير نشط"/>

                                                <Setter Property="Foreground"
                                                        Value="#999999"/>

                                                <Style.Triggers>

                                                    <DataTrigger Binding="{Binding IsActive}"
                                                                 Value="True">

                                                        <Setter Property="Text"
                                                                Value="نشط"/>

                                                        <Setter Property="Foreground"
                                                                Value="Green"/>

                                                    </DataTrigger>

                                                </Style.Triggers>

                                            </Style>

                                        </TextBlock.Style>

                                    </TextBlock>

                                </Grid>

                            </Border>

                        </DataTemplate>

                    </ListView.ItemTemplate>

                </ListView>

            </Grid>

        </Border>


        <!-- Footer -->

        <Grid Grid.Row="3"
              Margin="5,12,5,0">

            <TextBlock x:Name="StatusText"
                       Text="{Binding StatusMessage}"
                       Foreground="#666666"
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button x:Name="EditButton"
                        Content="تعديل"
                        Width="100"
                        Height="38"
                        Margin="0,0,8,0"
                        Click="EditButton_Click"/>

                <Button x:Name="DeleteButton"
                        Content="حذف"
                        Width="100"
                        Height="38"
                        Click="DeleteButton_Click"/>

            </StackPanel>

        </Grid>

    </Grid>

</UserControl>
